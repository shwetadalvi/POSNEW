package com.abremiratesintl.KOT.dbHandler;

public class SQLIteHelper{ /*extends SQLiteOpenHelper {

    public SQLIteHelper(Context context) {
        super(context, "users", null, 1);
    }

    @Override public void onCreate(SQLiteDatabase db) {

    }

    @Override public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion) {

    }

    @Override public synchronized SQLiteDatabase getWritableDatabase(String password) {

        File file = new File(Environment.getExternalStorageDirectory() + "/ABR/files/users.db");
        if (file.exists()) {
            return SQLiteDatabase.openOrCreateDatabase(file.getAbsolutePath(), MainActivity.getPassword(), null);
        }
        return null;
    }*/

}
